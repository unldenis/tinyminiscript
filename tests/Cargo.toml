[package]
name = "tests"
version = "0.1.0"
edition = "2024"

[dependencies]

rand = "0.8"

secp256k1 = { version = "0.29.1", features = ["rand"] }
bitcoin = { version = "0.32.7", features = ["rand"] }
tinyminiscript = { path = "../" }
